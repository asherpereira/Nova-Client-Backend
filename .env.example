# Generate strong random values before deployment.
NOVA_DB_PASSWORD=replace-with-a-long-random-password
NOVA_JWT_KEY=replace-with-at-least-64-random-characters

NOVA_HTTP_PORT=8080
# Docker Compose mounts this persistent volume automatically.
# Storage__LocalPath=/data/nova/uploads
